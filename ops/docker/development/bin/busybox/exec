#!/bin/sh

# ---------------------------
#
# busyboxで任意のコマンドを発行するシェルスクリプト
#
# ---------------------------

cd `dirname $0`/../../../../../

docker run -v $(pwd):/home -it --rm busybox /bin/sh -c "cd /home && $@"
